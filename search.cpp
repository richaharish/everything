#include<iostream>
#include<cmath>
#include<ctime>
#include<cstdlib>
using namespace std;

void linearsearch(a[],x,y)
{
   
   int n,posi,i;
   i=log
   
   while(n<16)
  {
     posi=rand()%y
  }
}

int main void
{
    int sampleno,i=0,j,arraysize,key,searchkey;

    arraysize=pow(2,16);
    int num[arraysize];
    srand(time(NULL));
    while(i<100)
   {
       for(j=0;j<arraysize;j++)
       {
           num[j]=rand();   
       }
       key=rand()%arraysize;
       searchkey=num[key];
       
   }
  return(0);
}
























